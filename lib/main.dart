import 'package:flutter/material.dart';

void main() {
  runApp(MaterialApp(
    title: 'Flutter Sandbox',
    home: Scaffold(
      appBar: AppBar(
        title: const Text('Flutter Sandbox'),
      ),
      body: Container(),
    ),
  ));
}
